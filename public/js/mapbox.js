// I decided to skip the map implementation for now, but I want to have the data-locations attribute ready to pass the locations data to the frontend for rendering the map later in the course. So I will just comment out the code that I will use to render the map later, but I will keep it in the codebase for now.

// const locations = JSON.parse(document.getElementById('map')?.dataset.locations);

// console.log(locations);
